#include "main.h"
/**
 *_abs - writen by assia
 *
 *@i: The number
 *
 * Return: absolute value of an integer
 */
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
_putchar('\n');
}
